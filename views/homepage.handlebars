<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Travel Plan App</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
</head>
<body>
    <header>
        <h1>Welcome to 4Travel</h1>
        {{> navigation}}
    </header>
    <section class="hero">
        <div class="hero-image">
            <img src="https://www.coindesk.com/resizer/z9ixf__1a6JqCUuf--f2Kk6JG-8=/1056x707/filters:quality(80):format(jpg)/cloudfront-us-east-1.images.arcpublishing.com/coindesk/2XKI4NL66ZEQ5MFKCRWRAWKMKE.jpg" alt="Travel" loading="lazy" />
        </div>
        <div class="hero-text">
            <h2>For your travel plans</h2>
        </div>
    </section>

    <main>
        {{#if logged_in}}
        <h2>Your Trips</h2>
        {{#if trips.length}}
        <div class="cards">
            {{#each trips}}
            <div class="card">
                <h3>{{this.trip_name}}</h3>
                <p>From: {{this.trip_origin}}</p>
                <p>To: {{this.destination}}</p>
                <p>Date: {{formatDate this.start_date}} - {{formatDate this.end_date}}</p>
            </div>
            {{/each}}
        </div>
        {{else}}
        <p>You have no trips planned.</p>
        {{/if}}
        {{else}}
        <p>Please <a href="/login">login</a> or <a href="/register">register</a> to view your trips.</p>
        {{/if}}
    </main>
</body>
</html>
